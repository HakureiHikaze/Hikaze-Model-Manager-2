# Not Implemented

- Pending promotion flow: trigger `modelCache.reset()` after promotion completes (UI/flow not yet wired).
- LoRA Power Loader full design/feature set is deferred; only basic node visibility is verified.
